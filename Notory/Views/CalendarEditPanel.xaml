<UserControl x:Class="Notory.Views.CalendarEditPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Notory.Views"
        mc:Ignorable="d">
  <Grid Background="Transparent">
    <!-- Header for Month and Year -->
    <TextBlock x:Name="MonthYearText" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" Margin="0,20,0,0" Foreground="#FF333333"/>

    <!-- Navigation Buttons -->
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,70,0,0">
            <Button x:Name="PrevMonthButton" Content="&lt;" Click="PrevMonthButton_Click" Width="40" Height="40" Foreground="#dde1e6" FontSize="18" Margin="5">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#764ba2" Offset="0.0" />
                        <GradientStop Color="#667eea" Offset="1.0" />
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="NextMonthButton" Content="&gt;" Click="NextMonthButton_Click" Width="40" Height="40" Foreground="#dde1e6" FontSize="18" Margin="5">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#764ba2" Offset="0.0" />
                        <GradientStop Color="#667eea" Offset="1.0" />
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
    </StackPanel>

    <!-- Days of the Week -->
    <UniformGrid x:Name="DaysOfWeekGrid" Rows="1" Columns="7" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,130,0,0">
            <TextBlock Text="MON" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#dde1e6"/>
            <TextBlock Text="TUE" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#dde1e6"/>
            <TextBlock Text="WED" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#dde1e6"/>
            <TextBlock Text="THU" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#dde1e6"/>
            <TextBlock Text="FRI" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#dde1e6"/>
            <TextBlock Text="SAT" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#dde1e6"/>
            <TextBlock Text="SUN" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#dde1e6"/>
    </UniformGrid>

    <!-- Calendar Grid -->
    <UniformGrid x:Name="CalendarGrid" Rows="6" Columns="7" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,160,0,0" />
  </Grid>
</UserControl>
